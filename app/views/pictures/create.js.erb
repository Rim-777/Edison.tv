<%  if @picture.new_record? %>
$('.notifications').html('<%= j render 'common/errors', resource: @picture %>');
<%else%>
var albumId = '<%= @picture.album_id %>';
$('#empty_album_'+ albumId).remove();
$('#album_' + albumId).append("<%= j render @picture %>");
setPopupGallery();
<%end %>
